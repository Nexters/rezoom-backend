language: java
jdk:
- openjdk8

branches:
  only:
  - master

services:
  - mysql

before_install:
  - mysql -e 'CREATE DATABASE [rezoom]'

cache:
  directories:
  - '$HOME/.m2'

script:
  - mvn clean install