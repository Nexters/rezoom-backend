# profile
spring:
  application:
    name: rezoom
  profiles:
    active: @activatedProperties@
  http:
    encoding:
      charset: UTF-8
  jpa:
    show-sql: true

# tomcat
server:
  tomcat.uri-encoding: UTF-8
  port: 8080
